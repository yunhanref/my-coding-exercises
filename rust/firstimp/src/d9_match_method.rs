/*
use std::ffi::CString;
use std::cmp::Ordering;
use rand::RngExt;
use std::io;
use std::ptr::null_mut;
use std::thread;
use std::time::Duration;
use std::io::repeat;
use rand::random_range;
use rand::rand_core::utils::read_words;
*/


//match
/*
fn main() {
    let mut question = String::new();
    println!("Bir sayı girin:");

    io::stdin()
        .read_line(&mut question)
        .expect("Okuma başarısız");

    let question: i32 = question.trim().parse().expect("Lütfen geçerli bir sayı girin");

    match question {
        n if n < 0 => println!("Negatif sayı girdiniz: {n}"),
        0 => println!("Sıfır girdiniz"),
        1..=10 => println!("1 ile 10 arasında bir sayı"),
        _ => println!("10'dan büyük bir pozitif sayı"),
    }
}
 */