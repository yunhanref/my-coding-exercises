/*
use std::ffi::CString;
use std::cmp::Ordering;
use rand::RngExt;
use std::io;
use std::ptr::null_mut;
use std::thread;
use std::time::Duration;
use std::io::repeat;
use rand::random_range;
use rand::rand_core::utils::read_words;
*/


//bilesik atama operatorleri (+=,*=...)
/*
fn main() {
    let mut a:u8 = 30;
    let b:u8 = 31;
    //a = a + b;  //out: 61
    // instead of this we can simply use +=
    a += b;
    println!("{a}"); //out: 61
}
 */